services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aionbit-web
    restart: unless-stopped
    ports:
      - "${SITE_PORT:-80}:80"
    env_file:
      - .env.runtime
